{
  "name": "tsx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
 "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "kill-port 5173 && npm run dev --prefix client --silent -- --port 5173 --strictPort",
    "dev:server": "npm run dev --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "kill-port": "^2.0.1"
  }
}
